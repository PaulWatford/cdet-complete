#!/usr/bin/env bash
# cdet -- launcher for the unified CLI (resolves its own directory, so it works from anywhere)
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$DIR/cdet.py" "$@"
