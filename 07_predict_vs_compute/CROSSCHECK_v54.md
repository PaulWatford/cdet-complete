# CROSSCHECK_v54 — the value channel's dressed −1, and the slice hierarchy of the cube

**Claim 1 (the −1 hunt, closed mechanistically).** On bipartite lattices the engine propagator obeys
exactly: off-diagonal G(−μ)(i,j,τ) = −ε_i ε_j G(μ)(j,i,−τ) (max err 2.5e-11 square, 1.8e-08 cube;
τ=0 off-diagonal 2.5e-16/4.7e-16), and on the equal-time diagonal the sum rule
G(−μ)(i,i,0) + G(μ)(i,i,0) = 1 (1.1e-15/8.9e-16). So G(−μ) = −D_ε G(μ)ᵀ D_ε + I|_equal-time: the
hunted −1 lives in the value channel but is dressed by a contact/density counterterm (the v46 d/dμ
object). Both naive clean-factor predictions FAILED by computation (per-config ratios non-constant,
std up to 5e3; the algebraically derived bare −1 off by up to 1.5e3) — recorded as the honest path to
the mechanism. No per-configuration −1 exists at fixed μ in either channel; the usable product is the
doping reflection μ ↔ −μ with exact computable corrections.

**Claim 2 (slice hierarchy).** Classifying sampled n=3 configs on the 4×4×4 cube (μ=0.5, 4000 configs)
by the affine-span dimension through the external site: 1d lines are 0.3% of configs but 5.6% of
|weight| (×18.5 concentration) at per-class R=0.224; 2d planes 20.5%/43.0% (×2.09) at R=0.089; 3d bulk
79.2%/51.5% (×0.65) at R=0.004. Mean |C| falls about an order of magnitude per added dimension.

**Reproduce.** `cd 08_2d_interacting && python3 value_channel_slices.py` → prints the dressed-PH
identity errors on the 2×2 square and 2×2×2 cube and the slice-classifier checks; final line
"value-channel + slices self-test: PASS". The slice-decomposition table is regenerated by
`slice_decomposition()` on the 4×4×4 (sampled; numbers above at seed 8).

**Scope (honest).** Claim 1 is exact (machine-precision identities + measured failure of the naive
forms). Claim 2 is one lattice, one order, one μ — a measured pattern and a hypothesis generator for
slice-stratified evaluation (v55), not yet a law. The physical sign wall is untouched by both.

**Frozen engine.** Untouched: `cd engine && make CC=gcc test` → 194/194; `cdet_order` constants
bit-identical (−0.5082750022348369  0.44040518398732875). All code wraps the engine via the validated
`cdet_port.py` (bit-identical to the frozen ring port at 0.00e+00).
