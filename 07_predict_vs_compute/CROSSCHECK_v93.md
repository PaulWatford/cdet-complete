# CROSSCHECK_v93 — the exponent-balance law: derived, frozen, measured

**Claims.** (1) The law: in a saturated window the stripped ⟨C⟩ is a finite polynomial in the
deviations e^(−β|μ−ε|) (weights ≤ 8); statics lock at exponent balances μ\* = (Σwᵢεᵢ)/(Σwᵢ),
approached as z = μ\* + ln(r)/(qβ), q = Σwᵢ — the one-spine form derived. (2) The field theorem:
statics live in the field generated by the spectrum (ℚ at L=6, ℚ(√2) at L=8) — the octagon chord
√(2+√2) excluded structurally at L=6; τ₀/τ₁ answered: no cross-anchor mixing. (3) L=8: the (1,1)
balance of (2√2−2, 2√2) equals 2√2−1 exactly (sympy), with the v84-rejected 1.707 as the naive
adjacent midpoint. (4) The finite menu in (1.80, 1.88) is {11/6, 13/7, 15/8}; the frozen
discriminator (predictions registered from z(48)/z(56) before measuring) came out z(36) =
1.8450 ± 0.0030, z(40) = 1.8457 ± 0.0046 → χ² 0.7 vs 9.5 — ~80:1 for 11/6; the global law
z = 11/6 + 2.67(54)/(6β) fits all four honest points at max 0.38σ. (5) Honest scope: 13/7
disfavored, not dead; ln r measured, not derived; the (2,3)-window probe inconclusive — that
window is suppressed (~30–50× below the (1,2) window), a queued datum; the A=0
parity/degeneracy rule remains open.

**Reproduce.** `cd 08_2d_interacting && python3 exponent_balance.py` (six gates: L=8 exact;
menu; frozen test; global law; field; live strong bracket; PASS ~45 s). Propagation:
`python3 csurrogate.py` (DEEPLAW test in the C gate), `python3 resonance_atlas.py` (audit A–I).

**Frozen engine.** Untouched: `cd engine && make CC=gcc test` → 194/194; constants bit-identical
(−0.5082750022348369  0.44040518398732875).
