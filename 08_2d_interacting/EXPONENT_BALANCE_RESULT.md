# The exponent-balance law (v93): the deep static derived, the field question answered, the discriminator frozen and measured

**The law.** By the v78 lemma, in a saturated window the stripped ⟨C⟩ is a *finite* polynomial in
the occupancy deviations dev = e^(−β|μ−ε|) (weights ≤ 8: 4×4 matrices × two spins). A zero locks
where two monomials balance, giving **μ\* = (Σ wᵢεᵢ)/(Σ wᵢ)** — an integer-weighted mean of the
lattice's own levels — approached as z(β) = μ\* + ln(r)/(qβ) with q = Σwᵢ (the one-spine form,
v85, now derived).

**The field theorem (the τ₀/τ₁ answer).** μ\* lies in the field generated by the spectrum: ℚ at
L=6, ℚ(√2) at L=8. The octagon chord √(2+√2) is not a weighted mean of integer levels —
**excluded structurally at L=6**. No cross-anchor mixing: each lattice's statics live in its own
anchor field. Verification at L=8 (sympy-exact): the (1,1) balance of the next-nearest pair
(2√2−2, 2√2) is 2√2−1 — the v84 static exactly — and the rejected 1.707 is the naive adjacent
midpoint. Both v84 facts accommodated.

**The finite menu and the frozen test.** In (1.80, 1.88) the menu is {11/6 (q=6), 13/7 (q=7),
15/8 (q=8)}; 15/8 was already dead (4.6σ). Predictions registered from the z(48)/z(56) pool
*before* measurement: 11/6 → z(36)=1.8475, z(40)=1.8461; 13/7 → 1.8369, 1.8389. Measured (honest
dense multi-draw): **z(36) = 1.8450 ± 0.0030, z(40) = 1.8457 ± 0.0046** → χ²: 0.7 vs 9.5 —
**~80:1 for 11/6**. Global law z = 11/6 + 2.67(54)/(6β) fits all four honest points at max dev
0.38σ (χ²/dof 0.09).

**Verdict.** The deep static is identified as the rational **11/6** — a τ₀-side (ℚ) constant, as
the field theorem requires — at ~2.6σ via the frozen test, with the v92 bridge verdict upgraded
from "NOT RIGID, one-of-many" to "derived 3-member menu + frozen-test selection." Honest scope:
13/7 disfavored, not dead (1.5–2σ); ln r ≈ 2.7 is measured, not yet derived (the residue formula,
queued); the (2,3)-window kill-shot probe was **inconclusive** — that window is *suppressed*
(|⟨C⟩| ≲ 0.04 e-9 throughout at β=20–28, ~30–50× below the (1,2) window; only non-reproducing
noise crossings), itself a datum (queued). The deeper "why" — which windows carry A=0 and which
balance is realized (the parity/degeneracy selection) — remains the head of the background-zero
program.

Reproduce: `python3 exponent_balance.py` (L=8 exact check; the menu; the frozen test; the global
law; the field gate; a live strong bracket; PASS ~45 s). Frozen engine untouched (194/194).
